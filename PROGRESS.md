# 프로젝트 진행 상황

이 문서는 오더정리 앱의 개발 진행 상황을 phase별로 기록합니다.

## Phase 1: 초기 프로젝트 설정 ✅

**완료일**: 2026-01-13

### 작업 내용
- CLAUDE.md 파일 개선
  - IIFE 모듈 패턴 설명 추가
  - 핵심 데이터 구조 문서화 (ParsedOrder, Patient)
  - 오더 단위 자동 계산 규칙 추가
  - 커스텀 테스트 프레임워크 사용법 문서화
  - SheetJS 외부 의존성 명시

### 커밋
- `1f66c91` - docs: CLAUDE.md 개선 - 아키텍처 패턴 및 테스트 정보 추가

---

## Phase 2: 파서 개선 - 시간 형식 및 치료사명 상속 ✅

**완료일**: 2026-01-13

### 작업 내용
- **파싱 분석 보고서 작성**
  - 실제 사용자 데이터 14줄 분석
  - 현재 파서 인식률 측정 (57%)
  - 4가지 주요 문제점 식별
  - 개선 우선순위 결정

- **시간 형식 자동 보정 기능**
  - 콜론 없는 4자리 숫자 형식 지원 (`1440` → `14:40`)
  - 범위 형식 지원 (`1440-1510` → `14:40-15:10`)
  - 괄호 안 시간도 자동 보정 (`RG2(1440-1510)`)
  - `normalizeTime()` 함수 추가
  - 15분 단위 오더 자동 계산과 호환

- **치료사명 상속 기능**
  - 이전 줄의 치료사명 자동 상속
  - 들여쓰기로 시작하는 줄 처리
  - `parseInput()`에 `lastTherapist` 상태 추가

- **테스트 케이스 추가**
  - 시간 형식 보정 테스트 5개
  - 치료사명 상속 테스트 4개
  - `testParserEnhancements()` 함수 추가

### 개선 효과
- 파싱 인식률: **57% → 85%+** (예상)
- 시간 형식 오류 자동 보정
- 들여쓰기 입력 패턴 지원

### 커밋
- `65cd167` - docs: 입력 데이터 파싱 분석 보고서 추가
- `c67ed86` - feat: 시간 형식 자동 보정 및 치료사명 상속 기능 구현

---

## Phase 3: 정의되지 않은 오더 코드 처리 ✅

**완료일**: 2026-01-13

### 작업 내용
- **경고 메시지 표시**
  - parseInput 결과의 warnings 배열 처리
  - 경고 메시지를 토스트로 화면에 표시
  - 경고가 있어도 출력은 정상 진행

- **자동 단위 적용**
  - 정의되지 않은 코드(예: COM)에 기본 2단위 자동 적용
  - 단위 확인 섹션 거치지 않고 바로 출력 가능
  - therapyType은 'unknown'으로 설정되어 경고 발생

- **테스트 케이스 추가**
  - 정의되지 않은 코드 파싱 테스트 4개
  - 경고 메시지 생성 검증
  - 출력 가능 여부 확인

### 동작 방식
1. COM 같은 미정의 코드 입력
2. 파싱 성공, 기본 2단위 적용
3. 경고 토스트 표시: "알 수 없는 오더 코드: COM"
4. 출력 양식에서 "치료" 유형으로 표시
5. 정상 출력 완료

### 사용자 경험
- ✅ 경고 메시지로 코드 누락 인지
- ✅ 출력은 정상 진행 (업무 중단 없음)
- ✅ 나중에 constants.js에 코드 추가 가능

### 커밋
- `ebe9f49` - feat: 정의되지 않은 오더 코드 경고 처리 및 자동 출력

---

## Phase 4: 출력 RM 정보 저장 기능 ✅

**완료일**: 2026-01-13

### 작업 내용
- **UI 추가**
  - [💾 RM 저장] 버튼 추가 (복사 버튼 옆)
  - 저장 성공 시 버튼 시각 피드백 (✓ 저장 완료)
  - 녹색 배경, 호버 효과

- **출력 파싱 기능** (parser.js)
  - `parseOutputForPatients()` 함수 추가
  - 출력 텍스트에서 `<RM숫자>` 패턴과 환자명 추출
  - 병동구분도 함께 파싱 (회복기, 전문 등)
  - RM?는 무시 (저장하지 않음)

- **저장 로직** (ui.js)
  - `handleSaveRm()` 함수 추가
  - 출력 텍스트 파싱 → Storage.applyPatients() 호출
  - 환자 수 업데이트
  - 성공/실패 토스트 메시지

- **테스트 케이스 추가**
  - 출력 텍스트 파싱 테스트 5개

### 사용 흐름
1. 입력: `홍길동/유혜영4/M(11:25-11:55)`
2. 출력: `<RM?>` 유혜영4님 운동 - M1
3. 사용자가 `<RM?>`를 `<RM3>`로 수정
4. [💾 RM 저장] 버튼 클릭
5. 환자 데이터에 저장: 유혜영4 = RM3
6. 다음에 유혜영4 입력 시 자동으로 RM3 출력

### 사용 시나리오
- **신규 환자 RM 수동 등록**: 환자 마스터에 없는 환자 입력 → RM? 출력 → 수동 수정 후 저장
- **담당 원장 변경**: 기존 RM3 → RM5로 수정 후 저장 → 업데이트

### 데이터 저장
- Storage: Patient = `{ name, room, ward }`
- localStorage에 영구 저장
- 기존 환자 정보는 업데이트됨
- Excel 업로드와 병행 사용 가능

### 커밋
- `c67c518` - feat: 출력 RM 정보 수정 및 저장 기능 추가

---

## Phase 5: 코드 품질 개선 및 버그 수정 ✅

**완료일**: 2026-01-13

### 작업 내용

#### 🔴 High Priority (버그 수정)

1. **시간 유효성 검증** (parser.js)
   - normalizeTime()에서 4자리 숫자 시간 검증 추가
   - 유효하지 않은 시간(2599 → 25:99) 차단
   - 시간 범위도 개별 유효성 검증

2. **음수/자정 넘어가는 시간 처리** (constants.js)
   - calculateMinutesFromTime()에 자정 넘어가는 시간 처리
   - 23:30-00:30 같은 케이스를 다음날로 계산 (90분)
   - 음수 결과 방지 로직 추가

3. **파싱 에러 사용자 표시** (ui.js)
   - 파싱 에러를 토스트로 표시 (기존: console.warn만)
   - 최대 3개 에러만 표시 + "...외 N개"
   - [추가]/[삭제] 구분 표시

#### 🟡 Medium Priority (품질 개선)

4. **단위 0 처리 개선** (formatter.js)
   - `order.unit || DEFAULT_UNIT` → 명시적 null/undefined 체크
   - unit=0일 때 올바르게 0으로 처리

5. **빈 환자명 검증** (formatter.js)
   - groupOrders()에서 빈 환자명 체크 및 건너뛰기

6. **RM 번호 유효성 검증** (patient.js)
   - normalizeRoomNumber()에 유효성 검증
   - "RMABC" 같은 잘못된 형식 차단

7. **escapeHtml 타입 처리** (constants.js)
   - 숫자 0이나 불린 false 올바르게 처리

### 개선 효과
- ✅ **안정성 향상**: 잘못된 입력에 대한 방어 로직 강화
- ✅ **사용자 경험**: 파싱 에러를 사용자에게 즉시 표시
- ✅ **데이터 무결성**: 유효하지 않은 시간/RM 번호 차단
- ✅ **코드 품질**: 타입 처리 명시적으로 개선

### 커밋
- `6cc5b33` - fix: 코드 품질 개선 및 버그 수정 (총체적 디버깅)

---

## Phase 6: 환자명 "님" 접미사 처리 ✅

**완료일**: 2026-01-15

### 작업 내용

#### 문제점
- 환자 이름에 "님" 접미사가 있는 경우 파싱 오류 발생
- 예: `박수진/조민수2님 : COM(08:30-09:00)`
- 증상: 치료사와 환자 이름이 혼동되어 출력됨
- 원인: 이름 패턴 `/^[가-힣]{2,4}\d*$/`이 "님"을 포함한 이름을 매칭하지 못함

#### 해결 방법

1. **normalizeName() 함수 추가** (parser.js)
   - "님" 접미사를 제거하는 정규화 함수
   - `"조민수2님"` → `"조민수2"`

2. **isPatientName() 개선**
   - 원본 이름과 정규화된 이름 모두 Storage에서 검색
   - 정규화된 이름으로 패턴 매칭

3. **구분자 파싱 로직 개선**
   - "/" 구분자 사용 시 양쪽 이름 모두 정규화
   - 정규화된 이름으로 치료사/환자 판단
   - 정규화된 이름을 최종 결과에 저장

### 개선 효과
- ✅ "님" 접미사가 있어도 환자명 정확히 인식
- ✅ 치료사/환자 혼동 문제 해결
- ✅ 실제 사용자 입력 패턴 지원 향상

### 테스트 케이스
- `박수진/조민수2님 : COM(08:30-09:00)` → 치료사: 박수진, 환자: 조민수2
- `　　　유혜영4님 : M(11:25-11:55)` → 환자: 유혜영4
- `이나예/김기연5님 : F(09:05-09:35)` → 치료사: 이나예, 환자: 김기연5

### 커밋
- `d2e1ec9` - fix: 환자명 "님" 접미사 처리 개선

---

## Phase 7: 이름 두 개 감지 로직 개선 및 콜론 구분자 강화 ✅

**완료일**: 2026-01-15

### 작업 내용

#### 문제점
- 슬래시 대신 콜론을 사용하면 환자 대신 치료사가 출력됨
- 예: `서편만/한효준님: RG2(1440-1510)` → 출력에 "서편만님" (치료사)
- 원인: 이름 두 개 감지 로직이 "/" 구분자에만 적용됨
- 두 번째 필드에 추가 구분자가 있으면 이름 추출 실패

#### 해결 방법

1. **detectDelimiter() 개선** (parser.js)
   - 콜론(`:`) 우선순위 추가: 탭 > `/` > `:` > 공백
   - 더 명확한 구분자 감지

2. **이름 두 개 감지 로직 확장**
   - "/" 뿐만 아니라 ":" 구분자에도 적용
   - 두 번째 필드에서 이름 부분만 추출:
     ```javascript
     const secondNameOnly = cleanSecond.split(/[\s:\/]+/)[0];
     ```
   - 예: `"한효준님: RG2"` → `"한효준님"` → normalizeName → `"한효준"`

3. **모든 이름 할당에 normalizeName() 적용**
   - 환자명, 치료사명 할당 시 항상 "님" 제거
   - 일관된 이름 정규화

4. **필드 처리 버그 수정** (2차 수정)
   - 문제: `"홍지민/김점자님: RG2"` 같이 필드가 2개일 때 파싱 실패
   - 원인: 이름 두 개 인식 후 `fields.slice(2)` → 오더 코드 사라짐
   - 해결: 두 번째 필드에서 이름 부분만 제거하고 나머지 유지
     ```javascript
     // "김점자님: RG2" → afterName: ": RG2" → cleanedAfterName: "RG2"
     const afterName = cleanSecond.substring(secondNameOnly.length).trim();
     const cleanedAfterName = afterName.replace(/^[:\s\/]+/, '').trim();
     if (cleanedAfterName) {
         fields = [cleanedAfterName, ...fields.slice(2)];
     }
     ```

### 핵심 로직
- **이름 두 개**: 첫 번째는 치료사, 두 번째는 환자
- **이름 한 개**: 환자 (치료사는 선택사항)

### 개선 효과
- ✅ "/" 또는 ":" 구분자 모두 정확히 처리
- ✅ "서편만/한효준님: RG2" → 치료사: 서편만, 환자: 한효준
- ✅ "홍지민/김점자님: RG2(08:30-09:00)" → 치료사: 홍지민, 환자: 김점자 ✅
- ✅ "김정자님: RG2" → 환자: 김정자 (이름 한 개)
- ✅ "최예원/김정원7/N" → 치료사: 최예원, 환자: 김정원7 ✅
- ✅ 이름 뒤에 다른 구분자가 있어도 정확히 인식

### 테스트 케이스
- `서편만/한효준님: RG2(1440-1510)` → 치료사: 서편만, 환자: 한효준
- `김희수/전동순님: RA2(14:40-15:10)` → 치료사: 김희수, 환자: 전동순
- `홍지민/김점자님: RG2(08:30-09:00)` → 치료사: 홍지민, 환자: 김점자 ✅
- `김정자님: RG2(15:15-15:45)` → 환자: 김정자
- `최예원/김정원7/N(15:50-16:20)` → 치료사: 최예원, 환자: 김정원7 ✅

### 커밋
- `6c2b9a3` - fix: 이름 두 개 감지 로직 개선 및 콜론 구분자 강화
- `f2ae8ee` - fix: 이름 두 개 감지 후 필드 처리 버그 수정

---

## Phase 8: 토큰 기반 파싱 리팩토링 및 시간 자동 계산 수정 ✅

**완료일**: 2026-01-15

### 작업 내용

#### 문제점
1. **구분자 의존성**: "님", ":", "/" 같은 구분자에 따라 파싱 결과가 달라짐
   - `박수진/조민수2님 : RM(08:30-09:00)` ≠ `박수진/조민수2/RM(08:30-09:00)`
2. **시간 자동 계산 실패**: `RM(08:30-09:00)` 입력 시 "단위 확인 필요" 경고
   - 원인: 전처리에서 괄호 분리 → 오더 코드와 시간 분리

#### 해결 방법

**1. 토큰 기반 파싱으로 완전 재작성** (`635524e`)

- **전처리**: 모든 구분자를 공백으로 통일
  ```javascript
  "박수진/조민수2님 : RM(08:30-09:00)"
  → "박수진 조민수2 RM(08:30-09:00)"  // 님 제거, / : → 공백
  ```

- **토큰화**: 공백으로 분리
  ```javascript
  ["박수진", "조민수2", "RM(08:30-09:00)"]
  ```

- **패턴 기반 순차 분류**:
  1. 시간 패턴 → time 변수
  2. 오더 코드 패턴 → orderCodes 배열
  3. 이름 패턴 → 첫 번째=치료사, 두 번째=환자
  4. 이름 하나만 있으면 환자로 처리

**2. 오더 코드+시간 합침 로직 추가** (`56559b4`)

- 문제: 전처리에서 괄호 분리 시 `RM (08:30-09:00)` → `["RM", "(08:30-09:00)"]`
- 해결: 오더 코드 뒤에 괄호 시간이 오면 자동 합침
  ```javascript
  if (i + 1 < tokens.length && /^\([\d:-]+\)$/.test(tokens[i + 1])) {
      orderToken = token + tokens[i + 1];  // RM + (08:30-09:00) = RM(08:30-09:00)
      i++; // 다음 토큰 스킵
  }
  ```
- 결과: `splitOrderCodes("RM(08:30-09:00)")` → `{code: "RM", time: "08:30-09:00"}`
- 단위 자동 계산: RM (15분 코드) + 30분 = 2단위

### 핵심 개선

**구분자 독립성**:
```
박수진/조민수2님 : RM(08:30-09:00)
박수진/조민수2님 RM2(08:30-09:00)
박수진/조민수2 RM(08:30-09:00)
박수진/조민수2/RM(08:30-09:00)
박수진/조민수2/RM2(08:30-09:00)
```
**모두 동일하게 파싱**: 치료사=박수진, 환자=조민수2, 오더=RM, 시간=08:30-09:00

**시간 자동 계산**:
- `RM(08:30-09:00)` → RM2 (30분 = 2단위) ✓
- `RM2(08:30-09:00)` → RM2 (명시적 단위 우선) ✓

### 테스트 케이스
- ✅ 10가지 입력 모두 동일한 출력: `조민수2님 운동 - RM2`
- ✅ 시간 기반 단위 자동 계산 정상 작동
- ✅ 명시적 단위 지정 시 우선 적용

### 커밋
- `635524e` - refactor: 구분자 독립적인 토큰 기반 파싱으로 완전 재작성
- `56559b4` - fix: 오더 코드 시간 자동 계산 버그 수정

---

## Phase 9: 괄호 안 시간 보호 및 평가 코드 인식 개선 ✅

**완료일**: 2026-01-15

### 작업 내용

#### 문제점 1: 괄호 안 콜론이 구분자로 인식
- `박수진/조민수2님 : RM(08:30-09:00)` 입력 시 "변환할 오더가 없습니다" 오류
- 원인: 전처리 단계에서 `(08:30-09:00)` 안의 `:` 를 공백으로 치환
- 결과: `RM(08 30-09 00)` → 오더 코드 인식 실패

#### 해결 방법: 플레이스홀더 기법 (`0ba833c`)

1. **전처리 전에 괄호 안 시간 추출**
   ```javascript
   const timeInParensMatches = [];
   let normalized = trimmedLine.replace(/\([\d:-]+\)/g, (match) => {
       const placeholder = `__TIME${timeInParensMatches.length}__`;
       timeInParensMatches.push(match);
       return placeholder;
   });
   ```

2. **구분자 치환 (괄호 안 시간은 이미 보호됨)**
   ```javascript
   normalized = normalized
       .replace(/님/g, '')
       .replace(/[\/:\t]+/g, ' ')  // 이제 괄호 밖 콜론만 치환
   ```

3. **플레이스홀더를 원래 시간으로 복원**
   ```javascript
   timeInParensMatches.forEach((time, index) => {
       normalized = normalized.replace(`__TIME${index}__`, time);
   });
   ```

#### 문제점 2: PHQ-9 같은 하이픈 포함 평가 코드 미인식

- `isEvaluationCode("PHQ-9")`가 false 반환
- 원인: 끝의 숫자를 먼저 제거 → `"PHQ-9"` → `"PHQ-"` → 매칭 실패
- 결과: 평가 오더가 치료 오더로 잘못 분류

#### 해결 방법: 2단계 체크 (`0ba833c`)

```javascript
function isEvaluationCode(code) {
    const upperCode = code.toUpperCase();

    // 1차: 원본 코드 그대로 체크 (PHQ-9 같은 하이픈 포함 코드)
    if (ALL_EVALUATION_CODES.some(c => c.toUpperCase() === upperCode)) {
        return true;
    }

    // 2차: 끝의 숫자 제거 후 체크 (ROM2 → ROM)
    const codeWithoutNum = upperCode.replace(/\d+$/, '');
    return ALL_EVALUATION_CODES.some(c => c.toUpperCase() === codeWithoutNum);
}
```

### 개선 효과
- ✅ 괄호 안 시간 형식 완전 보호 (콜론 유지)
- ✅ PHQ-9, PHQ-92 등 하이픈 포함 코드 정확히 인식
- ✅ 토큰 기반 파싱 안정성 향상

### 테스트 케이스
- `박수진/조민수2님 : RM(08:30-09:00)` → 정상 파싱 ✓
- `PHQ-9` → 평가 오더로 인식 ✓
- `ROM2` → 평가 오더로 인식 ✓

### 커밋
- `0ba833c` - fix: PHQ-9 같은 하이픈 포함 평가 코드 인식 버그 수정

---

## Phase 10: 신규 오더 코드 추가 및 UI/UX 개선 ✅

**완료일**: 2026-01-15

### 작업 내용

#### 1. 운동치료 코드 추가 (`30cc2cc`)

**추가된 코드**:
- **COM**: 운동치료 (30분 = 1단위)
- **M7**: 운동치료 (30분 = 1단위, 비급여 코드)

**constants.js 수정**:
```javascript
PHYSICAL_THERAPY_CODES.general: ['COM', 'CPM', 'F', 'M', 'M7', 'M15', ...]
UNIT_30MIN_CODES: ['COM', 'CPM', 'F', 'M', 'M7', 'M15', ...]
```

#### 2. 데이터 백업/복원 기능 추가 (`ab3b35c`)

**문제점**:
- `file://` 프로토콜 사용 시 폴더 경로 변경 = localStorage origin 변경
- 앱 업데이트 시 기존 데이터 (환자, 치료사, 설정) 손실

**해결 방법**:

1. **백업 기능** (storage.js)
   ```javascript
   Storage.exportData() // JSON 형식으로 전체 데이터 추출
   ```
   - 치료사 목록
   - 환자 마스터 데이터
   - 설정 (층, 직종, 선택된 치료사)
   - 버전 및 백업 일시 포함

2. **복원 기능** (storage.js)
   ```javascript
   Storage.importData(jsonString) // JSON 데이터 복원
   ```
   - 파일 형식 검증
   - 데이터 무결성 체크
   - 기존 데이터 덮어쓰기

3. **UI 추가** (index.html, ui.js)
   - 📤 데이터 백업 버튼
   - 📥 데이터 복원 버튼
   - 파일명: `orderApp_backup_YYYYMMDD_HHMM.json`

**사용 시나리오**:
1. 기존 앱에서 [📤 데이터 백업] 클릭
2. JSON 파일 다운로드
3. 새 앱 폴더 압축 해제
4. [📥 데이터 복원] 클릭하여 백업 파일 업로드
5. 모든 데이터 복원 완료

#### 3. 단위 확인 UI 개선 (`f813fe7`)

**문제점**:
- 단위 확인 섹션 내부에 "변환" 버튼
- 메인 "변환" 버튼과 중복
- 사용자가 어떤 버튼을 눌러야 할지 혼란

**해결 방법**:

**Before**:
```
[입력 영역]
[메인 변환 버튼]
[단위 확인 섹션]
  - 환자/오더 목록 + 단위 선택
  - [변환 버튼]  ← 중복!
[출력 영역]
```

**After**:
```
[입력 영역]
[메인 변환 버튼]  ← 하나로 통합!
[단위 확인 섹션]
  - 환자/오더 목록 + 단위 선택
[출력 영역]
```

**동작 방식**:
1. 첫 클릭: 파싱 → 단위 확인 필요하면 섹션 표시
2. 두 번째 클릭: 단위 적용 → 출력 생성

```javascript
function handleConvert() {
    // 단위 확인 섹션이 표시된 상태면 → 단위 적용 후 출력
    if (elements.unitCheckSection.style.display === 'block') {
        applyUnitsAndGenerate();
        return;
    }

    // 단위 확인 섹션이 없는 상태 → 파싱 수행
    // ...
}
```

### 개선 효과
- ✅ COM, M7 코드 사용 가능
- ✅ 앱 업데이트 시 데이터 손실 방지
- ✅ UI 직관성 향상 (버튼 혼란 제거)
- ✅ 사용자 경험 개선

### 커밋
- `30cc2cc` - feat: COM, M7 운동치료 코드 추가 (30분=1단위)
- `ab3b35c` - feat: 데이터 백업/복원 기능 추가
- `f813fe7` - refactor: 단위 확인 섹션 내부 버튼 제거, 메인 변환 버튼으로 통합

---

## Phase 11: [진행 예정]

### 계획된 작업
- 추가 메모 필드 지원 ("1:1가산체크란 없음" 등)
- 기타 개선 사항

### 상태
- 🔜 대기 중

---

## 진행 상황 범례

- ✅ 완료
- 🔄 진행 중
- 🔜 대기 중
- ⏸️ 보류
- ❌ 취소

---

## 참고사항

- 모든 업데이트는 이 파일에 기록됩니다
- Phase별로 구분하여 관리합니다
- 각 Phase는 완료 후 체크마크(✅)로 표시됩니다
